-                    p = d.ref,
-                    h = d.isIntersecting;
+                    h = p.ref,
+                    f = p.isIntersecting,
+                    m = Xi(ZA),
+                    v = hw("vod", m),
+                    y = 2 * v,
+                    g = (0, i.useState)(0),
+                    b = (0, r.A)(g, 2),
+                    _ = b[0],
+                    A = b[1],
+                    x = (0, i.useMemo)((function() {
+                        return n.slice(0, Math.min(30, v + _ * y))
+                    }), [y, v, n, _]);


+                    }), n.length >= 3 && _ < 2 && n.length > v + _ * y && x.length < 30 && (0, Vn.jsx)(GRe, {
+                        isExpanded: !1,
+                        blindText: t,
+                        onClickButton: function() {
+                            return A((function(e) {
+                                return e + 1
+                            }))
+                        }

-                    p = d.ref,
-                    h = d.isIntersecting;
+                    h = p.ref,
+                    f = p.isIntersecting,
+                    m = Xi(ZA),
+                    v = hw("vod", m),
+                    y = 2 * v,
+                    g = (0, i.useState)(0),
+                    b = (0, r.A)(g, 2),
+                    _ = b[0],
+                    A = b[1],
+                    x = (0, i.useMemo)((function() {
+                        return n.slice(0, Math.min(30, v + _ * y))
+                    }), [y, v, n, _]);


+                    }), n.length >= 3 && _ < 2 && n.length > v + _ * y && x.length < 30 && (0, Vn.jsx)(GRe, {
+                        isExpanded: !1,
+                        blindText: t,
+                        onClickButton: function() {
+                            return A((function(e) {
+                                return e + 1
+                            }))
+                        }

-f464abb3 2e5a576d 47bcbdf5



+4b62e59b 3bd4431d

